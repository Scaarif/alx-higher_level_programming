#!/usr/bin/node

/* Prints 3 lines (like 1-multi_langauages.js) using an array of strings and a loop */

// declare the array
const myArray = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
// print the array values, one per line
for (let i = 0; i < myArray.length; i++) console.log(myArray[i]);

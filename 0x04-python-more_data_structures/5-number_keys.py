#!/usr/bin/python3
def number_keys(a_dictionary):
    """ Returns number of keys in a dictionary
    Args:
        a_dictionary: the dict whose number of keys to return
    Return:
        number of keys
    """
    return len(list(a_dictionary))

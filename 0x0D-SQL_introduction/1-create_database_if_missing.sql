-- Creates a new database hbtn_0c_0 in the MySQL server
-- if the database already exists, the script shouldn't fail
-- not allowed to use the SELECT or SHOW statements

CREATE DATABASE IF NOT EXISTS hbtn_0c_0; 

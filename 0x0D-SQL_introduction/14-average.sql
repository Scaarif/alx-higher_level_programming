-- Calculates the score average of all recoreds in the table, second_table from the hbtn_0c_0 database in the MySQL server
-- The database name will be passed as an argument of the mysql command
SELECT AVG(score) AS average FROM second_table;

-- Removes all records with a score <= 5 in the table, second_table from the hbtn_0c_0 database in the MySQL server
-- The database name will be passed as an argument of the mysql command
DELETE FROM second_table WHERE score <= 5;

-- Displays the number of records with id = 89 in the table, first_table from the hbtn_0c_0 database in the MySQL server
-- The database name will be passed as an argument of the mysql command
SELECT COUNT(*) FROM first_table WHERE id = 89;
